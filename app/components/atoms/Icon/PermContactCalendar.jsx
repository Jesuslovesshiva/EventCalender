import {ReactComponent} from '@material-icons/svg/svg/perm_contact_calendar/round.svg'

import Icon from './index'

// istanbul ignore next
const PermContactCalendarIcon = props => (
  // eslint-disable-next-line react/jsx-props-no-spreading
  <Icon {...props} component={ReactComponent} />
)

export default PermContactCalendarIcon
