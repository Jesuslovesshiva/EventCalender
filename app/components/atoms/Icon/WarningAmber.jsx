import {ReactComponent} from '@material-icons/svg/svg/warning_amber/round.svg'

import Icon from './index'

// istanbul ignore next
const WarningAmberIcon = props => (
  // eslint-disable-next-line react/jsx-props-no-spreading
  <Icon {...props} component={ReactComponent} />
)

export default WarningAmberIcon
