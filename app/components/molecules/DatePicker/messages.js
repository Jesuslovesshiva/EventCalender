import {defineMessages} from 'react-intl'

const scope = 'molecules.date-picker'

export default defineMessages({
  date: {
    id: `${scope}.date`,
    defaultMessage: 'Datum',
    description: 'aria label date input'
  }
})
