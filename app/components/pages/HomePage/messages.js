import {defineMessages} from 'react-intl'

const scope = 'app'

export default defineMessages({
  someMessage: {
    id: `${scope}.someMessage`,
    defaultMessage: 'This is a message to show localization usage.',
  }
})
